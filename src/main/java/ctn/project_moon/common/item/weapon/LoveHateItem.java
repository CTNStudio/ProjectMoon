package ctn.project_moon.common.item.weapon;

import ctn.project_moon.common.item.EgoWeapon;

public class LoveHateItem extends EgoWeapon {
    public LoveHateItem(Properties properties, float maxDamage, float minDamage, float attackSpeed) {
        super(properties, true, maxDamage, minDamage, attackSpeed);
    }
}
